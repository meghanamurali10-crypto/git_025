# EXPERIMENT 6: COLABORATION AND REMOTE REPOSITORIES
## Project Management with GIT-Semester End Exam
To merge two branches using **git merge -m "Merging feature-branch" feature-branch"
This is done by git clone, git remote add origin <url>
